[build]
  command = "npm run build"
  # NO configurar publish aquí - el plugin @netlify/plugin-nextjs lo maneja automáticamente
  # Si tienes publish configurado en la UI de Netlify, bórralo desde ahí

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

